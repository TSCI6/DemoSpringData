version: '3.8'

networks:
  default:

services:
  db:
    image: mysql:5.7
    container_name: conference
    ports:
    - 3307:3307
    volumes:
    - "./.data/db:/var/lib/mysql"
    environment:
      MYSQL_ROOT_PASSWORD: hbstudent
      MYSQL_DATABASE: hbstudent